# Aether 后端开发文档

## 1 技术选型

主要组件：Java、SpringBoot、Maven、MySQL、Redis

> 根据需求可自行添加相关组件

包结构：`top.heyqing.aether`(后面如果部署访问域名是www.heyqing.top/aether)

```
top.heyqing.aether
├── controller          # 控制器
├── service             # 业务接口
│   └── impl            # 业务实现
├── repository          # 数据访问（JPA）
├── model
│   ├── entity          # 数据库实体
│   ├── dto             # 数据传输对象
│   └── vo              # 视图对象
├── config              # 配置类
├── security            # 安全认证
├── interceptor         # 拦截器
├── aspect              # AOP 切面
├── util                # 工具类
├── constant            # 常量
└── exception           # 异常处理

```

部署：Docker + Docker Compose

## 2 功能模块

### 2.1 用户模块

1. 本网站是由我个人使用（上传），供大家访问，所以用户模块只需要设置一个密码（用cryptex表示）密码默认：heyqing；虽然只有一个账户但登录安全性必须保障，暴力的非暴力的攻击都可以防御；数据库建不建表看你。
2. 游客用户：根据IP地址确定，通过工具包技术分析IP地址，确定具体位置，比如：中国·陕西·西安；这个最好建表，保存访问的具体信息等

### 2.2 文章模块

主要以blog等为主

文章标题、文章封面图、文章简介、阅读次数（更具IP统计的）、文章内容（HTML、Markdown等为主）等等

有分类、标签，可搜索（标题、简介、内容）仅文章可搜索

文章内容（文章页面）每个都可以具体独立的编辑（风格、样式、排版等），所以建表是一个还是多个你考虑清楚

### 2.3 视频|图片模块

选择一个样式好看、播放流畅（不吞画质的）、可变速的视频插件

图片有图集

视频和图片都可能在文章等（有富文本编辑的地方）出现

图片单独有图集，图集有封面和介绍

视频自动检测时长，可以添加关键时间节点

### 2.4 音乐模块

我上传音乐，有自定义合集和固定合集，自定义是用户（也就是我）自己创建的，固定合集多一个认证（就是一张专辑）

存储封面、歌词等

支持什么类型的文件要有说明

自动检测时长

播放时要有专门的播放特效（在播放页面以及播放后固定一个悬浮窗显示），通过ai生成，根据歌词、封面图等生成，生成后保存起来

可搜索（仅音乐），各个搜索模块分隔开来

### 2.5 书籍模块

主要放小说，有出版图书（别人的），也可以是自己的

封面、简介、作者、内容等

分章分节，出版图书就上传，上传（.txt文档）之后，通过ai进行自动分章分节，用户确认后进行上传

有分类标签（创建时尽可能全），ai自动推荐

### 2.6 存储模块

有本地存储和云存储（oss等有更推荐的也可以）两种；

我上传时可以选择；

大文件可以断点续传、分片上传，可以用零拷贝等技术

### 2.7 公告模块

有公告、动态、新闻等多个分类

一般就是文字+图片

### 2.8 订阅模块

用户可以进行订阅本网站，填入邮箱等即可

只推送文章和公告

有一个用户分布的问卷调查（可选），包含用户年龄、性别、职业、兴趣爱好等等用于用户分布分析

### 2.9 日志模块

保存我的操作日志

### 2.10 AI助手模块

一个本项目的ai助手（LangChain也好其他框架也行），基于本项目的ai助手

### 2.11 其他

若存到云上，删除时不仅仅要删除数据库中的字段数据还要删除云中的物理文件，修改文件也一样，先删除原文件再上传新文件最后修改覆盖数据库

## 3 数据库表

**每个字段要有注释，你生成**

## 附录

> 不要评论、点赞功能
>
> ai就是ai和agent、skill等，有本地模型和云两种：本地是ollama，云是DeepSeek
>
> 像分类、标签、问卷的兴趣爱好等在建表时自动生成数据，要尽可能的全面，特别是分类等这类数据
>
> 上传内容后自动推荐分类和标签
> 部分功能需要agent，就创建agent：如书籍分章分节等（agent使用java不好写就使用python）

