# Aether 开发阶段文档

> 本文档记录开发规范与开发阶段划分，作为开发过程的管理依据。
> 文档版本：v1.0（2026-08-24）

## 1 开发规范

### 1.1 代码规范（沿用原需求，编号整理）

1. 命名：规范化命名、见名知意
2. 代码清晰、明了，少语法糖，禁止屎山代码，代码注释清晰简洁，不图省事不写注释
3. 功能全面：每次编写代码前先确定开发方案的可行性、正确性
4. 安全无漏洞：在受到攻击时可以做出正确的方案
5. 代码测试：功能测试、单元测试、安全测试、防呆设计、压力测试等等（只要是代码可以实现且测试效果好的）
6. 测试结束后进行前后端联调
7. 统一的返回参数（见 BackEnd-Plan §3）
8. 开发阶段生成一些测试数据存入数据库，开发完成删除测试数据（假数据）
9. 前端的内容皆为后端获取，不在页面写死（如文章内容等）
10. 功能开发要符合企业级水平要深，前端和后端都要考虑全面（如：书籍的封面、作者、书名、分类、介绍、标签；txt 分段的段落开头空两格、段间半行距、插图等细节）

### 1.2 补充规范

| 规范项 | 规则 |
| --- | --- |
| Git 提交规范 | Conventional Commits：`feat(article): 支持文章独立样式`；类型 feat/fix/docs/style/refactor/test/chore；分支策略 main（仅合并，稳定可发布）+ dev（日常开发）+ feature/*（功能分支）；PR 单次改动 ≤ 400 行 |
| 代码审查 | 自查 → PR → 审查（含 AI 辅助 review）→ 前后端联调通过才合并；**改动 API 或表结构必须同步更新对应 Plan 文档**（BackEnd-Plan §5/§6、UI-Plan） |
| API 版本策略 | `/api/v1` 前缀；破坏性变更升 v2 并与 v1 并存一个版本周期后下线 v1 |
| 环境划分 | dev（本地 docker-compose）/ test（测试服务器）/ prod（www.heyqing.top/aether）；**所有配置环境变量外置**（BackEnd-Plan §11.2）；seed 测试数据仅 dev/test 执行 |
| 文档同步 | 任何与 Plan 文档不一致的实现，先改文档再改代码；阶段完成后更新本文件"当前阶段"状态；**push 含代码变更时必须伴随 Stage.md 更新**（提交记录表/进度/遗留，门禁 hook 强制校验） |
| 测试门禁 | git push 前必须 /test 全维度通过（门禁体系见 .claude/：code-tester agent + 6 维度 skill + 门禁 hook，**与业务 agent/ 无关**；纯文档改动放行，`--no-verify` 仅紧急使用）；commit 署名仅 dkb，不加 Claude Co-Authored-By |

### 1.3 当前阶段

- 项目状态：**阶段 3 进行中**（2026-08-27 代码完成并推送 dkb/dev，测试门禁 6 维度 PASS：后端 32/32 + 前端 lint/build + 安全/注释/功能/效率审查；联调通过，待站长浏览器视觉验收后打勾）；阶段 2 已完成（2026-08-27 站长浏览器视觉验收通过 ✅）；阶段 1 已完成（2026-08-25）
- 阶段 3 实现要点：图集/视频后端全链路（表/仓储/公开 API/管理 API）；签名 URL 播放 + Range 206（视频拖动）；图片上传 EXIF 抹除（JPEG 无损剥离/PNG eXIf 过滤）；时长探测 ffprobe 优先 + MP4/M4A 内置 mvhd 解析回退；storage_ref 引用登记 + 删除一致性（引用归零 status=0 → 事务提交后异步物理删除 → 补偿 job 每 10 分钟重试 3 天）+ uk_md5 复活（删除后同内容重传）；前端图集/视频列表与详情页（Lens 语义卡片、瀑布流 + pixel-image 懒加载、防下载预览层、ArtPlayer 倍速/清晰度/画中画/章节跳转）+ 主页推荐图集区块 + ProtectedImage 全站封面统一接入
- 阶段 3 说明：① seed 视频为程序构造的最小 MP4（仅 ftyp+moov/mvhd 无音视频轨，列表/时长/章节链路可联调，播放会失败并提示占位），真实视频待站长上传替换（管理端上传控件阶段 8）；② 本机无 ffprobe，时长探测走 MP4 内置解析回退（已测试验证），生产 backend 容器内置 ffprobe（阶段 9 部署时确认）；③ 图集/视频管理 UI（上传控件/节点编辑器）属阶段 8 管理端范围，阶段 3 交付 API + 集成测试；④ magicui 动效按既定安排延后统一引入（Lens/pixel-image 为 framer-motion 简化实现）
- 阶段 2 遗留问题：① ~~站长浏览器视觉验收~~ ✅ 已通过（2026-08-27）；② 文章搜索 LIKE 通配符 `%`/`_` 未转义（低危，仅影响搜索语义，无注入风险，见 BackEnd-Plan 附录 B）；③ 日统计 job 事务缺失 bug 已修（62fffbe，测试假阳性掩盖，回归通过）
- 前端动效说明（2026-08-27）：阶段 2 前端 UI 动效为**联调用语义简化版**（magicui 依赖未引入，Hero 悬浮信息面板/光环动画、AetherRing 加载与版权区动画、热门卡片排名动效与"阅读"按钮等以 framer-motion 简化实现或暂缺），**非最终效果**；待 UI 视觉打磨阶段引入 magicui 按 UI-Plan §7 清单统一补齐（站长已确认此安排）
- 说明：dev docker-compose 文件已就绪（deploy/docker-compose.dev.yml），Docker 环境按用户要求在项目末期统一安装；本地开发后端用 `local,dev` profile 组合连接真实 MySQL（application-local.yml，测试仍用 dev H2 内存库 + 内存 CacheStore）
- 提前完成：阶段 2 的设计基调确认已提前执行（2026-08-24，站长选定方案 D · 羊皮卷，展示页仅本地保留（design/moodboards/，不入库），tokens 与 LOGO 定稿见 UI-Plan §2/§3）
- 阶段 1 实现适配记录（BackEnd-Plan 已同步）：Hutool Captcha 替代 easy-captcha（javax.servlet 冲突）；CacheStore 抽象（Redis/内存双实现，dev 回退）；Boot 4.1 模块化适配（无 starter-aop 改用 spring-aop+aspectjweaver、MockMvc 测试模块 spring-boot-starter-webmvc-test、Jackson 3 tools.jackson 包名、Argon2 需显式引 bcprov-jdk18on、Hibernate 7 @Comment 已废弃故表注释以 schema-mysql.sql 为准）

### 1.4 提交记录（每次 push 含代码变更必须更新本表，门禁 hook 强制校验）

| 日期 | commit 范围 | 摘要 |
| --- | --- | --- |
| 2026-08-24 | 1158b65..6a1104d | 阶段 0 脚手架 + 测试门禁体系搭建 + 设计基调定稿（方案 D 羊皮卷） |
| 2026-08-25 | f864c3c..b0d8b7a | 阶段 1 后端基础（统一返回/登录三级防护/JWT/存储双实现/分片秒传）+ 修复项（security/storage） |
| 2026-08-27 | 47aa302..62fffbe | 阶段 2 代码（游客 IP 分析 + 文章模块前后端全链路）+ 文档同步 + 日统计 job 事务修复；门禁 6 维度 PASS |
| 2026-08-27 | 62fffbe..b2eefe5 | 测试门禁机制升级：文档同步门禁（push 含代码变更必须伴随 Stage.md，双 hook 规则 3 强制）+ 门禁文档同步（CLAUDE.md 第 5 条/README/Stage.md）+ 首次推送回退路径与 .gitignore 豁免修复 + BackEnd-Plan 附录 B LIKE 转义遗留 |
| 2026-08-27 | b2eefe5..HEAD | 阶段 3 图集+视频：后端全链路（album/album_image/video/video_chapter 表 + 公开/管理 API）+ 签名 URL Range 206 + 图片 EXIF 抹除（commons-imaging）+ 时长探测（ffprobe + MP4 内置回退）+ storage_ref 删除一致性（归零清理/补偿 job/uk_md5 复活）+ 前端图集/视频 4 页 + ArtPlayer + 防下载预览层 + ProtectedImage 统一接入 + 主页推荐图集区块 + seed 数据与 7 项集成测试（32/32 PASS）；BackEnd-Plan §1/§4.5/§6.2/§8.2 同步 |

## 2 开发阶段

阶段顺序：先打基础，再按模块"后端 → 前端 → 联调"推进。每阶段有明确的完成标准，**达成后打勾验收**。

### 阶段 0：项目脚手架

| 项 | 内容 |
| --- | --- |
| 目标 | 前后端双工程初始化，开发环境一键可起 |
| 任务要点 | backend 工程（Maven + Spring Boot 4.1 + 包结构 BackEnd-Plan §2）；frontend 工程（Next.js 16.3 + Tailwind 4 + shadcn/ui + basePath /aether）；python-agent 工程（FastAPI）；dev docker-compose（mysql/redis/ollama）；Git 仓库 + .gitignore + CI 基础流水线 |
| 完成标准 | 前端经 `/aether/api/health` 调通后端；Swagger 可访问；dev compose 一条命令启动全部中间件 |
| 依赖 | 无 |

### 阶段 1：后端基础

| 项 | 内容 |
| --- | --- |
| 目标 | 统一基础设施：返回/异常/日志/认证/存储抽象/context-path |
| 任务要点 | Result 与错误码（BackEnd-Plan §3）；@RestControllerAdvice；@OperationLog 切面；cryptex 登录 + 三级暴力防护（§4.1）+ JWT 双 token（§4.2）；StorageService 双实现（§8.1）；context-path 配置；seed 脚本（§6.4） |
| 完成标准 | 核心 service 单测覆盖；防暴力测试（限流/验证码/锁定）通过；分片上传-合并-秒传全流程测试通过；未授权访问 /admin/** 返回 20001 |
| 依赖 | 阶段 0 |

### 阶段 2：用户 + 文章（后端 → 前端 → 联调）

| 项 | 内容 |
| --- | --- |
| 目标 | 第一个端到端业务闭环 |
| 任务要点 | 游客 IP 分析入库 + 日统计 job（§4.6）；文章 CRUD + 独立样式 + 分类标签 + 搜索 + 阅读数 IP 去重；前端设计系统落地（UI-Plan §2，tokens 已定稿）；**设计基调确认 ✅（已提前完成：方案 D · 羊皮卷，展示页仅本地保留（不入库），tokens 与 LOGO 见 UI-Plan §2/§3）**；主页（Hero/公告/热门文章/推荐区块）；文章列表/详情页（滚动 5-8% 吸顶栏、RELATED ARTICLES 侧边栏） |
| 完成标准 | 搜索（标题/简介/内容）准确；滚动吸顶 5-8% 生效；悬停反色卡片与参考风格一致；假数据可完整走通主页 → 详情 → 相关文章 |
| 依赖 | 阶段 1 |

### 阶段 3：视频 + 图片（后端 → 前端 → 联调）

| 项 | 内容 |
| --- | --- |
| 目标 | 多媒体内容展示与防下载 |
| 任务要点 | 图集/图集图片管理；视频 + 时长探测（ffprobe）+ 关键时间节点；ArtPlayer 集成（倍速/清晰度）；签名 URL 播放（§4.4，Range 支持）；图片上传 EXIF 抹除（GPS 定位信息，§4.5）；图集详情（Lens 卡片、BlurFade 瀑布流、PixelImage 懒加载、图片预览不可下载） |
| 完成标准 | 断点续传可用；未授权直链 403；视频拖动进度正常；图集预览/防下载验证通过 |
| 依赖 | 阶段 1 |

### 阶段 4：音乐（后端 → 前端 → 联调）

| 项 | 内容 |
| --- | --- |
| 目标 | 音乐播放 + AI 特效 |
| 任务要点 | 合集（自定义/固定+认证）/单曲/歌词管理；时长检测；EffectConfig 生成（§7.3）；前端 MusicVisualizer 渲染引擎（Canvas + OffscreenCanvas + Worker）；音乐详情页 + AI 助手内音乐 tab；沙箱代码通道（iframe sandbox） |
| 完成标准 | 特效 config 全量走通（粒子/波形/节拍）；沙箱用例通过且校验失败自动回退 config 渲染；切歌插值过渡正常 |
| 依赖 | 阶段 1 |

### 阶段 5：书籍（后端 + python-agent → 前端 → 联调）

| 项 | 内容 |
| --- | --- |
| 目标 | txt 上传 → AI 分章 → 阅读器 |
| 任务要点 | python-agent 分章服务（启发式预分 + LLM 精调，§7.2）；分章任务状态机 + 管理端预览确认；书籍阅读器（封面翻页 → 第二页版权声明、目录抽屉、字号/阅读主题设置、进度记忆）；分段排版（段首空两格、段间半行距） |
| 完成标准 | 分章准确率抽查通过（≥95% 章节边界正确）；版权第二页翻页正常；断网降级（纯启发式分章可用） |
| 依赖 | 阶段 0、1 |

### 阶段 6：公告 + 订阅（后端 → 前端 → 联调）

| 项 | 内容 |
| --- | --- |
| 目标 | 公告轮播 + 订阅问卷闭环 |
| 任务要点 | 公告（公告/动态/新闻）管理；主页顺时针轮播（3-5 条）；订阅弹窗（同 IP 多邮箱、问卷可跳过）；问卷 IP 限 1 份 + 修改 2 次；邮件推送 job + 退订签名链接；问卷统计 |
| 完成标准 | 同 IP 已提交邮箱展示正确；问卷第 3 次修改被拒（30703）；邮件推送与退订链接验证通过 |
| 依赖 | 阶段 1、2 |

### 阶段 7：AI 助手（后端 → 前端 → 联调）

| 项 | 内容 |
| --- | --- |
| 目标 | AI 助手悬浮窗（对话 + 音乐双 tab） |
| 任务要点 | LangChain4j 双 provider + SSE 流式；对话限流（每分钟 10 条 + 每天 50 条，§7.5）；自动分类推荐（§7.4）；悬浮窗（CTRL+Space 唤醒、5 分钟无交互收起至边缘、对话/音乐双 tab）；管理端 AI 配置页（厂商/模型/开关/限额） |
| 完成标准 | 流式响应稳定；快捷键与自动隐藏符合需求；每日限额触发 30803；管理端切换模型即时生效 |
| 依赖 | 阶段 1、4 |

### 阶段 8：管理端（登录 → 各模块管理页）

| 项 | 内容 |
| --- | --- |
| 目标 | 站长工作台 |
| 任务要点 | cryptex 登录页（文字浪背景、验证码）；文章编辑（html/md/latex 多模式 + 预览 + 独立样式绑定）；各模块 CRUD（图集/视频/音乐/书籍/公告/订阅/分类标签）；上传双存储选择（本地/OSS）；ECharts 用户地域分布地图 + 概览统计；操作日志页；AI 配置页 |
| 完成标准 | 每个模块 CRUD + 预览功能测试通过；登录防护（限流/验证码/锁定）实测通过；地域分布地图数据正确 |
| 依赖 | 阶段 2-6 |

### 阶段 9：部署上线

| 项 | 内容 |
| --- | --- |
| 目标 | 生产环境稳定运行 |
| 任务要点 | prod docker-compose（六服务，BackEnd-Plan §11）；nginx path 部署（/aether/ + /aether/api/ + /aether/agent/）；域名与 HTTPS；robots.txt 全禁；数据备份 cron；生产改密（AETHER_CRYPTEX 强制） |
| 完成标准 | 生产环境全链路回归通过；**测试假数据已全部清理**；备份恢复演练通过 |
| 依赖 | 全部功能阶段 |

### 阶段 10：验收打磨

| 项 | 内容 |
| --- | --- |
| 目标 | 安全与性能达标，文档闭环 |
| 任务要点 | ZAP 安全扫描（高危清零）；JMeter 压测（文章详情 500 并发、2GB 分片上传、AI 对话并发）；前后端代码审查；四份文档与实现一致性校对 |
| 完成标准 | 高危漏洞清零；压测指标达标；文档与实现一致（API/表名/路由核对通过） |
| 依赖 | 阶段 9 |

### 阶段总览

| 阶段 | 内容 | 依赖 | 状态 |
| --- | --- | --- | --- |
| 0 | 项目脚手架 | 无 | ✅ 已完成（2026-08-24） |
| 1 | 后端基础 | 0 | ✅ 已完成（2026-08-25） |
| 2 | 用户+文章 | 1 | ✅ 已完成（2026-08-27 站长浏览器视觉验收通过） |
| 3 | 视频+图片 | 1 | 🔄 进行中（2026-08-27 代码完成已推送，门禁 PASS；联调通过：签名 200/Range 206/直链拒绝/SSR 渲染正常；待站长浏览器视觉验收） |
| 4 | 音乐 | 1 | ⬜ 待开始 |
| 5 | 书籍 | 0、1 | ⬜ 待开始 |
| 6 | 公告+订阅 | 1、2 | ⬜ 待开始 |
| 7 | AI 助手 | 1、4 | ⬜ 待开始 |
| 8 | 管理端 | 2-6 | ⬜ 待开始 |
| 9 | 部署上线 | 全部 | ⬜ 待开始 |
| 10 | 验收打磨 | 9 | ⬜ 待开始 |

> 阶段状态图例：⬜ 待开始 / 🔄 进行中 / ✅ 已完成
>
> 阶段 3-7 之间无相互依赖，可并行推进；每阶段按"后端 → 前端 → 联调"顺序执行（阶段 5 含 python-agent 先行）。
